services:
  # PostgreSQL Database
  - type: pserv
    name: fighter-pilot-db
    env: docker
    plan: free

  # Full-stack Web Service (Frontend + Backend)
  - type: web
    name: fighter-pilot-bash
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: fighter-pilot-db
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true
      - key: ADMIN_PASSWORD
        generateValue: true
    autoDeploy: false